import React,{useEffect,useState} from 'react';import api from '../api';export default function DSR(){const [events,setEvents]=useState([]);useEffect(()=>{api.get('/dsr/events').then(r=>setEvents(r.data))},[]);return(<div className='p-6'><h2 className='text-xl font-semibold mb-4'>DSR Events</h2><ul className='space-y-2'>{events.map(e=>(<li key={e.id} className='border rounded p-3'><div><b>Start:</b> {new Date(e.start_time).toLocaleString()} — <b>End:</b> {new Date(e.end_time).toLocaleString()}</div><div><b>Target:</b> {e.target_reduction_kw} kW</div><div className='text-sm opacity-70'>{e.note}</div></li>))}</ul></div>)}